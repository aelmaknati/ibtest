module.exports={friendlyName:"Update password",description:"Update the password for the logged-in user.",inputs:{password:{description:"The new, unencrypted password.",example:"abc123v2",required:!0}},fn:async function({password:password}){var hashed=await sails.helpers.passwords.hashPassword(password);await User.updateOne({id:this.req.me.id}).set({password:hashed})}};